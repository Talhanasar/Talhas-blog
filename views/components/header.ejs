<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="line bg-zinc-900 w-full h-4"></div>
    <div class="nav w-full flex justify-around items-center shadow-zinc-300 shadow-lg rounded-b-xl py-4">
        <div class="logo w-20">
            <img class="object-cover" src="https://shorturl.at/IXiy7" alt="">
        </div>
        <ul class="list-none flex gap-7">
            <% ['home', 'blog', 'about'].forEach(function(val) { %>
                <li class="<%= page === val ? 'text-red-300' : 'hover:text-zinc-300' %>">
                  <a href="<%= val === 'home' ? '/':'/'+val %>"><%= val.charAt(0).toUpperCase() + val.slice(1) %></a>
                </li>
              <% }); %>
        </ul>
        <% if(!currentUser){ %>
                <a class="py-1 px-4 rounded-md border-red-300 border-[2px] text-red-300 hover:text-red-500 hover:ring-red-700 hover:border-red-500" href="/user/login">Sign in</a> 
        <% } else {%>
            <a href="/profile/<%= currentUser._id %>" class="profile w-10 h-10 rounded-full overflow-hidden shadow-zinc-300 shadow-sm border-blue-500 border">
                <% if (currentUser.image.data.toString() != '') { %>
                    <img class="object-cover h-full w-full" src="data:<%= currentUser.image.contentType %>;base64,<%= currentUser.image.data.toString('base64') %>" alt="Profile Picture">
                <% } else { %>
                    <img class="object-cover h-full w-full" src="/image/profile.jpeg" alt="Default Profile Picture">
                <% } %>
            </a>
        <% } %>
    </div>
</body>
</html>
